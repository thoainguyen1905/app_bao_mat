import React from "react";
import {useModel} from "./Notification.hook";
import {View} from "react-native";
export const Notification =(props:any)=>{
    const {}=useModel(props)
    return(
        <View>

        </View>
    )
}
